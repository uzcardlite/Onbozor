[build]
builder = "nixpacks"

[deploy]
startCommand = "python -m app.bot_runner & uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
